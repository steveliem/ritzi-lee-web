##spring.datasource.url: ${JDBC_DATABASE_URL:}
##spring.datasource.hikari.connection-timeout=30000
## spring.datasource.hikari.maximum-pool-size=10

spring.thymeleaf.mode=HTML

logging.level.org.springframework=INFO

spring.profiles.active=production

server.port=${PORT:5000}

## Will deactivate configuration of default Spring whitepage
## because all 4xx and 5xx errors should redirect back to the
## index page.
server.error.whitelabel.enabled=false

## add one or more domains with comma separated list format
## cms.resource.domain=http://domain1.com,http://domain2.com,http://domain3.com
cms.domain=${CMS_DOMAIN:}
